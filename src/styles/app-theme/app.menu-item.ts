// @ts-nocheck

import palette from './app.pallete'

const menuItem = {
  styleOverrides: {
    root: {
      '&:hover': {
        backgroundColor: palette.primary[50],
      },
    },
  },
}

export default menuItem
